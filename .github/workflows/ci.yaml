name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-22.04
    container:
      image: ros:humble-ros-base
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y python3-pytest python3-colcon-common-extensions
          rosdep update --rosdistro=humble
          rosdep install --from-paths src --ignore-src -r -y || true

      - name: Build all packages
        run: |
          source /opt/ros/humble/setup.bash
          colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-Werror"

      - name: Run xacro tests
        run: |
          source /opt/ros/humble/setup.bash
          apt-get install -y ros-humble-xacro || true
          cd src/agv_sim_description
          python3 -m pytest test/test_urdf.py -v --tb=short || true

      - name: Validate xacro processing
        run: |
          source /opt/ros/humble/setup.bash
          apt-get install -y ros-humble-xacro || true
          xacro src/agv_sim_description/urdf/agv_sim.urdf.xacro > /dev/null
